// Exercise No.: 01 (Loop over the Array)
// Set up a for loop with a counter starting at 0.

// Solution Code:
const colors = ["red", "green", "yellow", "blue", "pink"];
for (let i = 0; i < colors.length; i++) {
        console.log (colors [i]);
    }